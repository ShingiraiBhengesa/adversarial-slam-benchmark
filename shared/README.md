# Shared Benchmark Code

This folder contains code that can be reused across SLAM systems.

Use this for patch generation, KITTI evaluation, diagnostics, plotting, data helpers, and cluster helpers.

System-specific scripts should stay inside the matching experiment folder.
