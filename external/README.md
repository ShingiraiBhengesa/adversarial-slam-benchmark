# External Code

This folder contains small external code used for evaluation.

## KITTI odometry devkit

- `kitti_odometry_devkit/`

This is the KITTI odometry evaluation devkit used for checking segment drift results.

The evaluator was lightly patched so it can run locally without the original email/reporting helper behavior.
